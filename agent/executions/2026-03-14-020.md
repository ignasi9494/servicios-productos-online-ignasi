# Execution #20 — 2026-03-14

## Tickets completed
- **#306** — Payment history: Pagar ahora CTA for pending payments
- **#307** — App preview/testing in dashboard with device toggle
- **#308** — Export/delivery flow
- **#400** — Admin dashboard panel (layout + home + projects + clients)
- **#600** — Landing CTA flow update (all CTAs → /cuestionario)

---

## Changes made

### #306 - Pagos enhancement (`src/pages/dashboard/Pagos.tsx`)
- Added `handlePay()` function that calls `createCheckoutSession()` and redirects to Stripe Checkout
- "Pagar ahora" button shown only for `status === 'pending'` payments
- Loading spinner on the payment button while Stripe session is created
- Stripe receipt ExternalLink only shown for `status === 'succeeded'` payments
- Imported `ArrowRight` and `createCheckoutSession` from stripe lib

### #307 - App Preview (`src/pages/dashboard/Preview.tsx`)
New page at `/dashboard/preview`:
- **Device selector toolbar**: Desktop (100%) / Tablet (768px) / Mobile (375px) with animated transitions (AnimatePresence)
- **iframe container**: renders the staging URL, transitions smoothly on device switch via `key={device}` + `refreshKey`
- **Fullscreen mode**: expands iframe to full viewport, back button overlaid
- **URL bar**: shows the preview URL in monospace
- **Refresh button**: increments refreshKey to reload iframe
- **Version badge**: "v1.0 – Última actualización: hace X horas"
- **Placeholder state**: friendly message when no preview URL is configured yet
- **Info bar**: explains how to use iterations for feedback

### #308 - Export/Delivery (`src/pages/dashboard/Entrega.tsx`)
New page at `/dashboard/entrega`:
- **Option A — Exportar código**: Download source code as ZIP. If final payment pending, shows amount and "Pagar y descargar" button via Stripe Checkout. Includes "code is 100% yours" disclaimer.
- **Option B — Mantener en nuestra plataforma**: 3 hosting plans (Básico 49€/Pro 99€/Premium 199€) with feature lists, "Más popular" badge on Pro, animated card selection, "Activar plan" button for Stripe subscription
- **NotReadyState**: shown when project is not yet completed, links back to dashboard home
- Three USP cards: Security, Performance, Support
- Footer note: code always belongs to the client, can export anytime

### #400 - Admin Panel (`src/pages/admin/`)
Four new files:

**AdminLayout.tsx**:
- Fixed sidebar with amber Shield badge ("Panel Admin")
- Mobile overlay drawer with hamburger toggle
- Admin-only guard: if `profile.role !== 'admin'`, shows access denied page
- Link back to client dashboard
- Sticky mobile header with hamburger menu

**AdminHome.tsx**:
- 4 KPI cards: total projects, pending proposals, in development, clients
- 3 quick stats row: questionnaires this week, unread messages, completed projects
- Pipeline summary bar with colored dots per status
- Recent projects list (last 8) with client name, date, plan badge, status badge
- Quick actions section linking to Proyectos and Mensajes

**AdminProjects.tsx**:
- Full-width project table with search + status filter dropdown
- Each row: project name, client name, date, price, plan, status badge
- Link to `/admin/proyectos/:id` (detail page coming soon)
- Alert banner when projects have `status === 'pending_proposal'`

**AdminClients.tsx**:
- Card grid with client initials avatar, company, phone, sector, project count badge
- Search by name/company/sector
- Pulls from `profiles` table filtered by `role === 'client'`

**AdminPlaceholder.tsx**:
- Reusable placeholder for Chat/Pagos/Analytics/Configuración sub-routes

### Routes added to `main.tsx`
- `/dashboard/preview` → Preview
- `/dashboard/entrega` → Entrega
- `/admin` → AdminLayout (ProtectedRoute)
  - `/admin` → AdminHome
  - `/admin/proyectos` → AdminProjects
  - `/admin/clientes` → AdminClients
  - `/admin/mensajes`, `/admin/pagos`, `/admin/analytics`, `/admin/configuracion` → AdminPlaceholder

### DashboardLayout sidebar updated
- Added "Vista previa" (Monitor icon) → `/dashboard/preview`
- Added "Entrega" (Package icon) → `/dashboard/entrega`

### #600 - Landing CTA updates
All `href="#questionnaire"` references replaced with `<Link to="/cuestionario">`:
- **Hero.tsx**: Primary CTA changed from "Descubre qué plan..." (→ #pricing) to "Iniciar cuestionario gratuito" (→ /cuestionario); secondary CTA now goes to #pricing
- **Navbar.tsx**: Both desktop and mobile "Empezar proyecto" buttons use `<Link to="/cuestionario">`
- **Pricing.tsx**: All 3 plan CTA buttons use `<Link to="/cuestionario">`
- **AddOns.tsx**: "Consulta con nosotros" link points to `/cuestionario`; renamed imported `Link` icon to `LinkIcon` to avoid collision with react-router `Link`
- **Retainer.tsx**: All "Contratar plan" buttons link to `/cuestionario`
- **Footer.tsx**: "Empezar ahora" (was mailto) replaced with `<Link to="/cuestionario">` "Iniciar cuestionario gratuito"

---

## Files created
- `src/pages/dashboard/Preview.tsx` — App preview with device toggle
- `src/pages/dashboard/Entrega.tsx` — Export/delivery flow
- `src/pages/admin/AdminLayout.tsx` — Admin sidebar + layout
- `src/pages/admin/AdminHome.tsx` — Admin dashboard home with KPIs
- `src/pages/admin/AdminProjects.tsx` — Project management list
- `src/pages/admin/AdminClients.tsx` — Client management grid
- `src/pages/admin/AdminPlaceholder.tsx` — Under construction placeholder

## Files modified
- `src/main.tsx` — Added 9 new routes
- `src/pages/DashboardLayout.tsx` — Added 2 sidebar links + icons
- `src/pages/dashboard/Pagos.tsx` — Pagar ahora button for pending payments
- `src/components/Hero.tsx` — CTA to /cuestionario
- `src/components/Navbar.tsx` — CTAs to /cuestionario
- `src/components/Pricing.tsx` — CTAs to /cuestionario
- `src/components/AddOns.tsx` — CTA to /cuestionario + icon rename
- `src/components/Retainer.tsx` — CTAs to /cuestionario
- `src/components/Footer.tsx` — CTA to /cuestionario

---

## Commit hashes
| Commit | Description |
|--------|-------------|
| `af81dfd` | Agent: add app preview and export/delivery pages (#307 #308) |
| `bd23cf9` | Agent: build admin dashboard panel with projects and clients (#400) |
| `aeb50d6` | Agent: add routes for Preview, Entrega, and Admin panel |
| `bcf62c3` | Agent: update all landing CTAs to route to /cuestionario (#600) |
| `1700fe9` | Agent: enhance Pagos page with 'Pagar ahora' CTA for pending payments (#306) |

---

## Build result
`npm run build` — SUCCESS (8.64s). No errors, only expected 786 KB chunk-size warning.

---

## Test results
- Build passes cleanly
- All new routes accessible via react-router nested structure
- Admin layout has admin-only guard at component level
- CTA links confirmed replaced with react-router Link components
- Pagos handlePay connects to existing createCheckoutSession function

---

## New issues discovered
- Admin route `/admin/proyectos/:id` (project detail page) is linked but not yet implemented → next session: #401
- Entrega.tsx uses mock constants (`PROJECT_COMPLETED = true`) — in production should read from `project.status`
- Preview.tsx uses empty `PREVIEW_URL` constant — in production should come from `project.preview_url` column

---

## Next session priorities
1. **#401** - Admin project management (detail view, status changer, questionnaire viewer, proposal editor)
2. **#402** - AI proposal generator admin tool
3. **#403** - Email notification system
4. **#501** - Add extras/add-ons pricing detail
5. **#503** - Update FAQ with new platform questions
