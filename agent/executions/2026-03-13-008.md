# Execution 008 — 2026-03-13

## Ticket
**#103** — Set up Stripe integration for payments

## What was done

### 1. Package installed
- `@stripe/stripe-js` v3 (client-side Stripe.js loader)

### 2. `src/lib/stripe.ts` — NEW
Frontend Stripe utilities:
- `getStripe()` — lazy loads Stripe.js with publishable key from env
- `createCheckoutSession(params)` — calls the Supabase Edge Function to get a Checkout session URL, then redirects
- `formatAmount(amountCents, currency)` — formats cents to locale string (e.g. 150000 → "1.500,00 €")
- `paymentTypeLabel(type)` — maps 'deposit'/'final'/'maintenance' to Spanish labels
- `paymentStatusInfo(status)` — maps payment status to Spanish label + CSS color class

### 3. `supabase/functions/create-checkout-session/index.ts` — NEW
Deno Edge Function:
- Accepts: `{ projectId, paymentType, amount, projectName }`
- Creates Stripe Checkout Session (payment mode for deposit/final, subscription mode for maintenance)
- Returns `{ url }` — the hosted Stripe checkout page URL
- Requires env secrets: `STRIPE_SECRET_KEY`, `APP_URL`

### 4. `supabase/functions/stripe-webhook/index.ts` — NEW
Deno Edge Function:
- Verifies Stripe webhook signatures (`stripe-signature` header)
- Handles `checkout.session.completed` → inserts into `payments` table + updates project status
  - deposit → status `in_development`
  - final → status `delivered`
- Handles `payment_intent.payment_failed` → marks payment as `failed`
- Handles `customer.subscription.deleted` → marks last maintenance payment as `refunded`
- Requires env secrets: `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`

### 5. `src/pages/dashboard/Pagos.tsx` — UPDATED
Real payment history page (was placeholder):
- Fetches all payments for current user's projects from Supabase
- Summary cards: total pagado, nº transacciones, pendientes
- Payment list with: status icon, payment type label, project name, date, amount, link to Stripe dashboard
- Success/cancelled banners from URL params (after Stripe redirect)
- Refresh button

### 6. `supabase/functions/README.md` — NEW
Deployment instructions for Edge Functions (supabase CLI commands, secrets setup, webhook registration)

### 7. `.env.example` — UPDATED
Added `VITE_STRIPE_PUBLISHABLE_KEY` and notes for Supabase secrets

## Files changed
- `src/lib/stripe.ts` — NEW (94 lines)
- `src/pages/dashboard/Pagos.tsx` — UPDATED (170 lines)
- `supabase/functions/create-checkout-session/index.ts` — NEW (76 lines)
- `supabase/functions/stripe-webhook/index.ts` — NEW (96 lines)
- `supabase/functions/README.md` — NEW
- `.env.example` — UPDATED

## Build
✅ `npm run build` — success (warning: bundle > 500KB, expected)

## Commit
`6649e35` — Agent: set up Stripe integration for payments (#103)

## Pending (requires manual action)
- Set `VITE_STRIPE_PUBLISHABLE_KEY` in Vercel environment variables
- Deploy Edge Functions to Supabase with `supabase functions deploy`
- Set Supabase secrets: `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `APP_URL`
- Register webhook endpoint in Stripe Dashboard

## Status
✅ Complete (client code + Edge Functions ready; requires manual Stripe/Supabase configuration)

## Next Session Priorities
- **#200** — Create AI chatbot UI shell and page (Questionnaire page, high value)
- **#103 follow-up** — Add PaymentButton component to proposal acceptance flow (once proposals exist)
